// create a component that will display a default message if ranked info is not available
import React from 'react';

const UnrankedComponent = () => (
    <div>
        No ranked info
    </div>
);

export default UnrankedComponent;