{
  "name": "summoner-central",
  "version": "1.0.0",
  "description": "This is a simple tool to collect a League of Legends player's summoner statistics, along with simple game information",
  "main": "index.js",
  "scripts": {
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "start:dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
